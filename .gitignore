wasm/lib/
# wasm/get-wasi-sdk.sh installs beside the repository, not in it -- but
# --dir can point anywhere, and 600 MB of toolchain must never become a
# commit.
wasi-sdk-*/
dist/
.vite/
*.o
node_modules/
package-lock.json
test/__screenshots__/
.DS_Store
